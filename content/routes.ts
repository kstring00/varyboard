/**
 * Public routes for sitemap.xml. Add a route here when its page ships.
 * Priority/changeFrequency are hints only.
 */
export const routes: { path: string; priority: number }[] = [
  { path: "/", priority: 1 },
];
