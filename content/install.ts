/**
 * Written installation steps. Eric supplies these; the install page shows the video
 * and the "before you start" facts until the array has entries.
 */
export interface InstallStep {
  title: string;
  body: string;
}

export const installSteps: InstallStep[] = [];
