import { InlineIntake } from "@/components/plan/InlineIntake";

/** Homepage section #find-your-plan: the three-step intake, inline. Replaces the body-map builder. */
export function FindYourPlan() {
  return <InlineIntake />;
}
